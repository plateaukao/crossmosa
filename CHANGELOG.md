# 變更紀錄 / Changelog

本檔記錄 CrossMosa 每個公開版本的變更。
格式:每個版本一節，分「功能 / 閱讀與效能 / 穩定性 / 在地化 / 實作備註」五類。

> Written in Traditional Chinese. An English summary of the fork lives in the second half of
> [`README.md`](README.md).

---

## 2.1.0 (2026-09) — internal build 237

這是 2.1 系列的正式版，整合直排閱讀、原版 X4 支援與穩定性修正。

### 功能 (Features)

- **Sharik EPUB 接收**：主畫面新增「接收 EPUB」，可透過同一個 Wi-Fi 網路或裝置熱點接收 Android Sharik 傳送的 EPUB；接收完成後可直接開啟閱讀。
- **外部字型即時更新**：從網頁上傳字型後，主設定與書內文字設定都會立即刷新字型清單。
- **X3/X4 待機壁紙工具**：壁紙產生器支援指定 X3 或 X4 的直向尺寸。

- **重製兩套字型包**：`sd-fonts.zip` 與 `sd-fonts-large.zip` 都已更新。
  內文字型與大字版字型現在完整涵蓋拉丁擴充 A／B，共 336 個碼位。歐洲語言的重音與變音字母，例如 ř、ł、ő、ș，不再落到備援字型或顯示成方塊。
- **移除 `GuanKiapTsingKhai-90`**。這套字型是舊版旋轉螢幕搭配預先旋轉字型的偽直排方案；在 2.1 的真正直排下選用，文字反而會躺著。要使用楷書請改選 `Iansui`（芫荽）。
  2.0.1 的字型包仍保留這套字型，供尚未升級的使用者使用。

### 閱讀與效能 (Reading and performance)

- **修正直排側轉西文的基線**。整串側轉的英文單詞、縮略詞與數字串，現在共用同一條基線，不再因為各詞有沒有上伸部、下伸部而上下跳動。
- **新安裝預設關閉抗鋸齒**。每頁可省去兩趟灰階刷新，翻頁約快半秒。
  這項變更只影響沒有 `settings.json` 的全新 SD 卡，或自行刪除設定檔後重新啟動的情況；既有使用者會保留原本的設定，這一版沒有設定遷移。
  抗鋸齒仍可正常使用，需要時可到「設定 → 閱讀器 → 文字設定 → 樣式 → 文字反鋸齒」打開。

### 已知的限制 (Known limitations)

- **`.txt` 還不支援直排，排在後續版本。**
- **`.xtc` 不支援直排**。XTC 每頁儲存的是預先算好的點陣圖，方向在轉檔時就已決定，結構上無法在閱讀器內切換。
- **一本書只有一個方向**。
- **直排時圖片與 `<hr>` 分隔線獨占一頁**。
- **段落額外間距在直排沒有效果**。

### 升級注意 (Upgrade notes)

- ⚠️ **每本書第一次打開會重新排版一次**，厚書要多等幾秒。排版快取版號 121 → 122；**閱讀進度不受影響**。
- **字型包這次有更新。** beta.1 所寫的「沿用 2.0.1，不必重新下載」已不再適用。
  建議已下載過 2.0.1 字型包的使用者重新下載這一版的 `sd-fonts.zip` 或 `sd-fonts-large.zip`。舊字型包仍可使用，但缺少完整的拉丁擴充字元。
- **想退回 2.0.1 隨時可以**。

---

## 2.1.0-beta.1 (2026-09) — internal build 235

> ⚠️ **這是測試版，不是正式版。** 它加了一個很大的新功能（直排），
> 而新功能的第一輪一定還有沒被發現的問題。
> 想穩定使用請留在 [2.0.1](https://github.com/anki630/crossmosa/releases/tag/v2.0.1)；
> 願意幫忙試、也願意在出狀況時回報的，歡迎刷這一版。
> 回報請開 issue，附上 SD 卡上的 `crash_report*.txt`（如果有）與截圖。

這一版的主角是**直排**——中文書由右往左、一欄一欄往下讀，跟紙本一樣。

### 功能 (Features)

- **⭐ 直排閱讀（縦書き）**。設定 → 閱讀器 → 文字設定 → 版面 → **文字方向**，三個選項：

  | 選項（畫面上的順序） | 意思 |
  |---|---|
  | 橫排 | 一律橫排 |
  | 直排 | 一律直排 |
  | **依出版社** | 由書自己決定。**預設值**——升級上來的人也會拿到，因為這是新設定 |

  「依出版社」讀的是 EPUB 裡的翻頁方向標記（`page-progression-direction`）。
  實測的結論是：**凡是內文樣式真的指定直排的書，這個標記都在**——所以它不會漏掉該直排的書。
  ⚠️ **反過來會誤判**：有些書標了右翻，內文其實是橫排的（多半是印刷版直排、
  轉檔時只留下翻頁方向）。**遇到就手動改成「橫排」**，也請順手回報書名。
  ⚠️ 改文字方向會讓那本書**重新排版一次**，不是瞬間的——請等它跑完再翻頁。

- **直排的細節是照規範做的**，不是把橫排轉個方向：
  - **句讀居中**——`。` `、` `，` 放在字格的正中央，不是右上角。
    這是**教育部《重訂標點符號手冊》**的寫法（手冊裡九個地方都寫「占一個字的位置，居正中」），
    也是台灣書的作法；中國大陸與日本才放右上角。
  - **直排專用字形**——`「」` `（）` `《》` `【】` 這些會換成直排的樣子，不是把橫的轉過來。
  - **行尾點號懸掛**——一行的最後如果是 `、` `，` `。`，讓它掛在版心外面，
    這樣**字距完全不用動**。日文排版叫「ぶら下げ組」，clreq 也說繁體直排可以這樣做。
  - **禁則**——標點不會跑到欄首、開引號不會落在欄尾。
  - **段首縮排兩格**；書裡本來就打了全形空格的不會變成三格。
  - **數字併成一格**（縦中横）——像列點的 `1.` `2.`、`第10章` 的 `10`，
    會併在一個字格裡橫著排，不會上下拆開。
  - **英文單詞整串側轉**，單一字母與縮略詞（`AI` `PDF`）則保持直立。
  - **箭頭、破折號、製表符號會跟著轉向**，依 Unicode 官方的直排資料（UAX #50）判斷。

- **欄距三檔**（緊／標準／寬），與行距在同一個地方。

- **側邊按鍵「依版面」**，並成為預設。直排的書是右翻的，
  所以**翻頁方向自動對調**——左邊那顆變下一頁。橫排的書維持原樣。
  - 升級上來的人也會自動換成這個（原本停在舊的預設值「上頁/下頁」的話）。
    **橫排書的行為逐位元組不變**；當初自己改成「下頁/上頁」的人不會被動到。
  - ⚠️ **選單的上下移動不受影響**，換的只有翻頁。

- **設定整理**。文字方向與欄距搬進「文字設定」，跟字型、字級、行距、邊距放在一起；
  設定 → 閱讀器最外層只留入口與螢幕方向、圖片。

### 已知的限制 (Known limitations)

這些是**這一版就知道、刻意先不做**的，不是等著被回報的 bug：

- **`.txt` 與 `.xtc` 不支援直排**，只有 EPUB 有。那兩種走的是另一套排版程式。
- **一本書只有一個方向**。有些書的書名頁、獻詞是橫排、內文才直排，
  我們會整本用同一個方向。
- **直排時圖片與 `<hr>` 分隔線獨占一頁**，還不會嵌在欄裡。
  ⚠️ 例外：**兩邊都不超過兩個字寬的小圖會被略過**（有些書的每個小標題前面都掛一顆
  項目符號圖，一顆就吃掉一整頁）。大圖照舊獨占一頁。
- **段落額外間距在直排沒有效果**。

### 穩定性 (Stability)

- **修正截圖有時會失敗、什麼都沒發生**。書名很長時檔名會被截在 UTF-8 字元中間，
  而且第一次截圖時 `/screenshots` 資料夾建不出來。
- **修正韌體更新途中偶發的重開機**（WiFi 訊號弱、上傳很慢時）。
  看門狗的逾時與上傳等待剛好同一個量級，慢一點就被判定當掉。

- **修正註腳／連結清單結尾的黑方塊，並把可顯示的字數加倍**。
  連結文字存在一個定長欄位，原本會切在漢字中間留下半個字元；長度從 32 加到 64 bytes
  （約 21 個漢字），真的被截到時補省略號。**橫排也受惠**——目錄頁的超連結走同一條路。

- **從目錄或註腳跳到某一章明顯變快**。原本每次跳頁都先排 8 頁才肯畫第一頁，
  而中文＋SD 字型每頁要 100–200 毫秒——實測有 4.7 秒的空白等待。
  現在只排到要顯示的那一頁；真的慢的時候會出現「整理中」提示（原本那個提示永遠不會出現）。

### ⚠️ 升級注意 (Upgrade notes)

- **每本書第一次打開會重新排版一次。** 這一版動了排版的輸出格式，舊的排版快取一律作廢。
  厚一點的書會多等幾秒，**閱讀進度不受影響**。
- **想退回 2.0.1 隨時可以**：照平常的方式刷回去就好。設定與進度都留著，
  直排相關的設定會被忽略。

### 實作備註

- 直排的符號旋轉表由 Unicode 官方的 `VerticalOrientation.txt`（UAX #50）**機械產生**，
  不是手寫清單。手寫清單的失敗模式是「永遠少一個」—— 這個功能第一版就是那樣，
  於是 `→ ↑ ↓ ⏎ │ ▌ ⤴ ➔` 全部立著不動。

---

## 2.0.1 (2026-09) — based on CrossPoint 1.5.0, internal build 205

修正版。主要修兩件事:**書名和檔名裡的黑方塊**，還有**逛 OPDS 書單時偶爾會當機**。

### 在地化 (Localisation)

- **系統字型多收了 496 個罕用字**。檔案清單和書名裡本來是黑方塊的字（像「羣」「倂」）
  現在都有字了。
  - 挑字的方法也換了。以前是照 BIG5 的字頻表排名次挑，那份表反映的是一般用字，
    不是**書名**用字;現在改成掃整個電子書庫的內文，看真正會出現在書名和檔名裡的是哪些字。
  - ⚠️ 系統字型是編在韌體裡的，**要重刷才會生效**。書的內文不受影響（那走 SD 卡字型）。

### 穩定性 (Stability)

- **修正逛 OPDS 書單時偶發的當機**。畫面在畫書單的同時，抓下一頁的程式會把書單換掉，
  兩邊撞在一起就會讀到已經清掉的資料。現在換書單一定等畫完才做。
  - ⚠️ 這種當機沒辦法主動重現，只能靠後續使用觀察。還有發生的話，
    請把 SD 卡上的 `crash_report*.txt` 附上來。
- **修正字型解壓縮的兩個潛在當機點**（上線前複查時找到的，還沒有人回報中招）:
  一個是查不到某個字該在哪一塊時會讀到界外，另一個是資料對不上時會寫爆緩衝區。
  兩處都改成先檢查、有問題就跳過那個字，不要拖垮整頁。

### 字型包（另外下載，不必刷機）

- **五套內文字型全部重做**，補上中文電子書實際會用到、但先前漏掉的符號:
  圈圈數字 `① ㈠`、注音 `ㄅㄆㄇ`、**直排標點 `︿ ﹀ ﹃ ﹄`**、方框幾何 `─ ■ ○ ☆`、
  希臘字母，以及 **CJK 擴充 A 整個區塊**（隨機抽 823 本書來看，有 70 本會用到）。
  - 抽 60 本掃描:**漢字一個都不缺**。做這件事之前缺 143 個字元，60 本裡有 58 本會碰到。
  - 漏掉的原因不是字型沒有這些字，是產生字型時**沒有把那些區塊列進去**。
  - ⚠️ **2.0.1 之前下載過字型包的請重新下載**，否則那些字仍然是方塊。
- **新增大字版字型包**:黑體與明體，字級 **24 / 26 / 28**。複製到 SD 卡即可，**不必刷韌體**。
  - 停在 28 是刻意的。字太大反而不好讀 —— 真正決定舒不舒服的是**一頁剩幾個字**:
    22pt 一頁約 138 字、28pt 剩 85 字、36pt 只剩 52 字。做過 36，用幾天就拿掉了，
    一頁五十幾個字等於一直在翻頁。

### 與上游的差異 (Fork notes)

- 內建 UI 字型檔頭裡的產生指令改用相對路徑，不再寫出本機的完整路徑。
- 字型區塊的查找從逐一比對改成二分搜尋。**現在感覺不出差別**（內建字型只有 13 個區塊），
  留著是因為區塊一多就會有感。

---

## 2.0.0 (2026-09) — based on CrossPoint 1.5.0, internal build 196

這一版的主角是**一塊不會動的螢幕**。有人刷完舊版 CrossMosa 之後，畫面停在「更新已完成」——
機器其實還活著（SD 卡上會冒出資料目錄），但面板一個像素都不換。

原因是新一批次的 Xteink X3 換了面板控制器：**UC8279**（舊批次是 UC8253）。
舊韌體只會跟舊控制器說話，於是看起來像死機。這一版會在開機時辨識控制器，
並為 UC8279 校正灰階——這是這個 major 版本存在的理由。

基底也整個換到上游 **CrossPoint 1.5.0**，再把 CrossMosa 既有的繁中與閱讀優先改動搬回來。
因為拿掉了若干功能（見下），版號進到 2.x。

### 功能 (Features)

- **支援新批次 X3 的 UC8279 面板控制器**。開機自動辨識；新機器刷完畫面會正常更新。
  - 一併修正 UC8279 上灰階偏淡、待機壁紙過黑的問題（同一套對映）。
- **對齊上游 CrossPoint 1.5.0**。閱讀引擎、設定架構與諸多修正跟到 1.5.0 基底，
  再疊上 CrossMosa 的繁中介面、SD 中文字型與記憶體護欄。
- **EPUB 內嵌 GIF**（只顯示首幀）現在可以畫出來，不再一律變成佔位框。
  （主畫面封面路徑仍不走 GIF。）
- **粗體閱讀**回到設定與文字設定頁（整本內文加粗，標題不受影響）。
- 新增介面主題 **Formosa Pro**。可選主題為：**Formosa**、**Formosa Extended**、**Formosa Pro**。

### 閱讀與效能 (Reading & performance)

- **從休眠醒來快了約 0.8 秒**。以前醒來會先把開機 logo 推上面板（電子紙的一次完整刷新），
  再立刻換成書頁；現在要回書就不畫它，畫面從待機圖直接變成你在讀的那一頁。
- **換章等待明顯縮短**。章首若是整頁圖，先前常要空等十秒左右才出字；
  現在改成**先出文字、圖稍後補上**（第一次還沒有圖片快取時會先看到佔位，隨即自動重繪補圖）。
  實測換章關鍵路徑大約從十秒級降到約 1–3 秒。
- **換章後整章翻頁較順**。背景排版改為把當章頁面排完，不再只「跟著眼前幾頁」走；
  字型預取也較不容易被卡住。
- **圖片顯示較穩**：版面階段較少因記憶體吃緊而靜默丟圖；直式大圖與整頁圖的夾限／縮放路徑有修。
- **字型**：換章重排前會真正釋放上一章占用的字型快取，降低「記憶體不足／索引失敗」彈窗；
  換章時也會清掉已滿的字寬快取，避免後面章節的字一直打到 SD 而變慢。

### 穩定性與修正 (Fixes)

- **記憶體吃緊時比較不會直接重開機**。多處會失敗的配置改成可失敗並降級
  （顯示失敗、放棄當次排版），避免一路走到強制中止；也避免把殘缺頁面寫進 SD 快取變成永久壞頁。
- 連 Wi-Fi／開 OPDS 前再度卸載 SD 內文字型騰出記憶體，降低連線後被踢回主畫面的情況。
- **設定 → 清除閱讀快取**改為兩段確認：可選**只清除快取（保留進度）**或**清除快取並重設進度**。
  （閱讀選單裡「清除本書快取」同樣可保留進度。）

### 介面 (UI)

- **設定 → 系統 → 裝置資訊**：顯示螢幕驅動晶片（UC8253／UC8279）與機器序號。
  回報問題時可以直接告訴我們機器是哪一種。
- 主題選項收斂為 Formosa／Formosa Extended／Formosa Pro（見上）。
- 若你先前的設定還停在已退役的主題編號，開機載入時會自動遷到上述三者之一。

### 字型 (Fonts)

- **五套 SD 內文字型全部重做，現在都是完整漢字涵蓋**（約 21,000 字）。
  以前黑體只有 15,383 字、芫荽只有 5,366 字，台語文、古文與人名裡的冷僻字會顯示成黑框；
  隨機抽 60 本書掃描，重做前黑體有 13 本會出現黑框，現在**五套都是 0**。
- **新增「原俠正楷偽直排版」**（字型選單裡叫 `GuanKiapTsingKhai-90`）。
  字形預先轉了 90 度，選它再把螢幕轉成橫向，就能由上而下、由右而左直排讀中文。
- 已下載過字型包的請重新下載 `sd-fonts.zip`。整包 116 MB，解開後**只複製你要用的那幾套即可**。

### 其他

- 內部診斷紀錄更細（開機各階段耗時、封面產生失敗的原因分類、喚醒失敗的原因碼），方便追問題。

### 升級注意 (Upgrade notes)

- 主要受惠對象是新批次、刷舊韌體後畫面凍住的機器；但換章速度與字型涵蓋的改善對所有人都有感。
- **兩種螢幕晶片都已在實機上刷過**：UC8279（新批次）是維護者自己的機器，UC8253（舊批次）由使用者回報成功。
  型號是開機時實際讀晶片判斷的，不用自己選。
- **每本書第一次打開會重新排版一次**（版面快取格式相對 1.2.0 已變更）。閱讀進度、書籤、
  Wi-Fi 憑證會保留；含圖的章節第一次可能稍慢，之後走快取。
- **移除的功能**（這是 major 版號的理由，請先看清楚再刷）：
  - **SMB 檔案分享**（iOS「檔案」App 直接管 SD）：先前公開版預設就不編進發佈韌體；
    這一版連原始碼一併拿掉，**無法再打開編譯開關編回來**。請改用網頁傳檔、Calibre、OPDS 或拔卡複製。
  - **BLE 翻頁遙控器**：同上，發佈韌體本來就沒有；這一版原始碼也不再保留，無法自編加回。
  - **RoundedRaff 主題**（以及 Classic）：設定裡不再出現。
- 從 1.x 升級建議走既有的 SD／網頁更新流程（見 README）。刷完請在開機畫面或設定頁確認版號為 `2.0.0`。

### 與上游的差異 (Removed / disabled vs upstream)

相對上游 CrossPoint 1.5.0，本分支大致延續既有取捨（騰 flash 給中文字型、閱讀優先），並在這一版**收得更乾**：

- 繼續不收：29 種非英／繁 UI 語言、KOReader 進度同步、字典、線上 OTA、非英文斷字表、內建斜體與多餘內建閱讀字級等。
- **這一版額外拿掉／關死**（見升級注意）：SMB2、BLE 翻頁遙控器、RoundedRaff／Classic 主題。
- **保留**：Calibre 無線推書、網頁設定與傳檔、WebDAV、OPDS、傾斜翻頁、截圖、按鍵重配、待機畫面、SD 卡韌體更新。

---

## 1.2.0 (2026-08) — based on CrossPoint 1.4.1, internal build 130

這一版的主角是**一種打不開的書**。有讀者回報某個 EPUB 開起來不是第一頁，
而是直接顯示「全書完」——書明明在，目錄是空的，一頁內容都讀不到。

原因在 EPUB 的目次檔(`content.opf`)。XML 允許元素名帶命名空間前綴，`<package>` 也可以
合法地寫成 `<ns0:package>`，兩者意思完全一樣。CrossMosa 的解析器只認裸名，於是整份目次
收到 **0 筆章節**，而「0 筆章節」在閱讀器裡的表現就是「你已經讀完了」。

這個前綴的寫法是 EPUB 規格允許的，只是不常見。常見的打包工具都輸出裸名，
所以 550 本回歸語料裡一本都沒有，先前完全沒察覺。什麼情況下會產生帶前綴的檔案，我還沒查出來
（可能經過某些工具重新編輯或打包過），但成因不重要:**規格允許的寫法就該支援**。

### 穩定性與修正 (Fixes)

- **開書直接顯示「全書完」**（目次檔用了命名空間前綴）。五個 XML 解析器現在一律比對
  **local name**（前綴後面的部分），不再只認裸名。前綴可以是任何字串，寫死一份前綴清單
  只能修掉當下這一個實例。
  - 同時修好的還有:`container.xml` 帶前綴會讓**書完全打不開**、目次檔帶前綴會**沒有目錄**。
    這三處的成因相同，而先前只有 OPF 那一處有實際案例。
- **書名、作者、語言不見了**。上一項的修法有個陷阱:`dc:title` / `dc:creator` /
  `dc:language` 這三個目標字串**本身就自帶前綴**，只把元素名去前綴、卻不動目標字串，
  會讓它們永遠對不上:書照樣能讀，但書名和作者就這樣不見了。三者一併處理了。
- **主畫面的封面變成空框**。這是上面那個 bug 的後遺症，而且它自己會留下來:
  書「壞掉」的那段期間，主畫面產生縮圖失敗，程式會寫一個 **0 byte 的檔案**當作
  「這本書沒有封面，別再試了」的記號。解析器修好之後，那個記號還在，封面就永遠不會回來。
  - 現在只要書其實有封面，就會刪掉記號重新產生。**不需要手動刪任何東西**，
    已經中招的書刷完自己會復原。
- **快取存在不等於快取有效**。上面兩個問題是同一個形狀:一次失敗被寫成了合法的快取產物，
  而檢查只看「檔案在不在」。兩者都住在 SD 卡上，所以重刷舊版韌體也清不掉。
  這一版對兩者都改成檢查內容而不只是檢查存在。

### 介面 (UI)

- **閱讀選單重整**，從 14 項變成 12 項，並依「開這個選單的目的」重排:
  先是位置（選擇章節、註腳、書籤、跳至指定進度），接著是邊看邊調的排版（字級、行距、粗體），
  最後才是一次性的設定與工具。
  - **移除「回主畫面」與「截圖」**。這兩項的功能都還在，拿掉的是重複的入口:
    閱讀中按 Back 鍵本來就回得了主畫面(預設短按;若你把「Back 短按進檔案瀏覽器」
    打開了則是長按)，而截圖有全域組合鍵 **電源 + 下**，在任何畫面都有效。
    走這兩條原本的路徑比開選單快，也少兩次全螢幕刷新。
  - 螢幕一頁放得下 12 列，所以多數書一頁看完不必捲動。橫向只放得下 7 列，
    因此前 7 項刻意排成「閱讀中真的會伸手」的那些。
- **移除設定 → 操作裡的「藍牙翻頁器」**。藍牙功能在更早的版本就已經關閉，但這個條目
  漏了跟著移除，於是它顯示在設定裡、選下去卻**完全沒有反應**。

### 升級注意 (Upgrade notes)

- **沒有變更任何快取格式**。既有的書不會重新排版，閱讀進度、書籤、設定都不受影響。
- 如果你之前遇過「開書就全書完」或「封面空框」，刷完之後**不需要手動刪除 SD 卡上的
  `/.crossmosa` 資料夾**——這一版會辨認出壞掉的快取並自動重建。
- 閱讀選單的項目順序變了。如果你習慣用位置記憶去按，第一次會需要看一眼。

---

## 1.1.0 (2026-08) — based on CrossPoint 1.4.1, internal build 126

這一版的主角是 **`.txt` 檔**。1.0.0 讀 txt 是勉強能用:一本 73 萬字的中文小說要等
**73 分鐘**才打得開第一頁，而且翻頁還要三秒多。原因是它沿用了 EPUB 的思路——先把整本書
排版完、算出總頁數，才顯示第一頁。純文字檔沒有章節結構，這個前置作業買不到任何「顯示第一頁」
需要的東西，卻要付整本書的代價。

1.1.0 把它整個換掉:**只排「當下這一頁」**。

> 版號說明:1.0.0 的版號字串是 `1.0.0 (cp1.4.1 build 115)`。1.1.0 起改成純版號，
> 上游基底與內部建置號改記在這裡的節標題，不再佔用開機畫面那一行。

### 功能 (Features)

- **`.txt` 有閱讀選單了**（公開 repo issue #1）。四項:**跳到指定進度**、**調整字級**、
  **調整螢幕方向**、**回到主畫面**。
  - 字級與方向在舊架構下不敢放——改一次就要重建整本索引、等 73 分鐘，而且進度會漂掉。
    串流之後兩者都只是「用同一個位元組位移重排當前頁」，約一秒。
  - **刻意不放行距與粗體**:那兩項在 txt 的繪製路徑上是無效的(行高只取字型 advanceY、
    字重寫死)，放進去會變成「看起來有反應但畫面不變」，比沒有更糟。未來版本會補上。
- **英文斷字回歸**（設定 → 閱讀器 → 英文斷字，預設關閉）。先前為了騰出 flash 空間給中文字集
  而移除，社群回報有人用這個韌體讀英文書。非英文的九種語言維持移除。

### 閱讀與效能 (Reading & performance)

- **`.txt` 開書:73 分鐘 → 約 1 秒**（實測 745,717 位元組的中文小說）。不再預先排版整本書。
- **`.txt` 翻頁:約 3.2 秒 → 0.59 秒**（命中字型預取時;未命中約 1.0 秒）。
  0.59 秒裡有 **0.44 秒是電子紙面板的物理時間**，軟體成本只剩約 0.15 秒。
- **`.txt` 進度改用百分比**顯示，不再顯示頁碼。txt 的頁碼是用平均頁長外推的**估計值**，
  而位元組位移算出來的百分比是精確的。EPUB 維持頁數。
- 內部:每頁的字寬查詢從 406,342 次降到 101 次;斷行改為單趟前向掃描。

### 穩定性與修正 (Fixes)

- **整頁圖片有時完全不顯示，只剩一個空白框**。版面把圖縮放到整頁高度，卻放在已被上緣邊距
  推下來的位置，於是超出畫面底部十幾個像素——繪製時的邊界檢查會靜默放棄，而佔位框沒有那道
  檢查照畫。現在會縮到該頁真正剩下的高度（放得下的圖完全不受影響）。
- **直式書封不顯示**。圖片來源尺寸上限是 3,145,728 像素(2048×1536)，而電子書的直式封面
  （例如 1600×2244 = 3,590,400）輕易就超過，超過時整張圖在**版面階段**就被移除。
  上限放寬到 6,291,456(2048×3072)——那道上限限制的是解碼時間而不是記憶體，
  實測約 1.1 秒／百萬像素，而且每張圖只付一次（之後走像素快取）。
- **`.txt` 翻頁時按鍵被吞掉**:繪製約一秒，期間再按一次翻頁會變成原地重畫（看起來像沒反應）。
  現在會排隊，畫完自動前進。
- 修正 `.txt` 繪製與主任務之間的競態，最壞情況會誤判成「已經是最後一頁」而跳回主畫面。
- 圖片失敗的診斷補完:先前有三條失敗路徑（圖檔不存在／大小為 0／沒有對應的解碼器）
  **完全不留任何紀錄**，症狀是白框而記錄檔什麼都查不到。

### 介面 (UI)

- **沒有封面的書，佔位圖重新設計**:原本是下三分之二一整塊實心黑，改成靠左一條細書脊線
  加置中的圖示。電子紙的介面不塗滿背景，這塊黑是最後一處例外。

### 升級注意 (Upgrade notes)

- 刷完之後**每本書第一次打開會重新排版一次**（版面快取格式 34 → 36），閱讀進度不受影響。
  含整頁插圖的書，那張圖第一次顯示會慢幾秒，之後走快取就正常。
- **`.txt` 的閱讀進度改存位元組位移**（原本存頁碼）。舊版寫的進度會在第一次開書時自動換算，
  不需要手動處理。

---

## 1.0.0 (2026-08) — based on CrossPoint 1.4.1, internal build 113

第一個公開版本。以下是這個分支從上游 1.4.1 分出去之後累積的全部改動，
按主題整理，不是開發流水帳。

### 功能 (Features)

- **OPDS 書庫瀏覽整套重做**:已造訪的頁面存到 SD 卡的上一頁 / 下一頁堆疊，
  往回或往前跳幾頁都不必重新連線抓取。
- OPDS 返回時保留游標位置，不會每次退回都跳回第一項。
- OPDS 長按翻頁改成「一次長按 = 一個動作」;游標已經在最底再長按一次，才載入伺服器的下一頁。
- OPDS 下載完成後直接詢問是否立刻開書。
- OPDS 書單改用與其他清單共用的繪製元件，樣式與最近閱讀一致。
- **閱讀選單快捷**:字級、行距、粗體閱讀三項可在閱讀中直接切換，選定即生效。
- 新增**粗體閱讀**開關（整本內文加粗，標題不受影響）。
- 最近閱讀清單與續讀卡顯示閱讀進度百分比。
- 多頁清單的標題列顯示頁碼 `3/12`。
- **QR 書摘**修正:改用 ISO 18004 的真實容量上限，先前產生的是格式合法但掃不出來的碼。
- **待機壁紙:50 張世界名畫**（Release 資產 `crossmosa-1.0.0-wallpapers.zip`）。
  全部取自 Wikimedia Commons 的公共領域作品，逐張為 X3 的 4 階灰階面板挑選與調校——
  管線是「灰階 → autocontrast → 縮到 528×792 → Floyd-Steinberg dither → 疊標籤 →
  寫 2-bit 原生 BMP」，**2-bit 是關鍵**:8-bit 會被韌體用它自己的 Atkinson 重新 dither，
  電腦端的演算法就白做了。好幾張名畫因為在 4 階下失去對比而換掉(例如莫內《印象·日出》
  整張靠色相對比，兩色亮度幾乎相同，灰階化之後畫面就消失了)。
  策展清單、轉檔工具與逐張的取捨理由在 `wallpapers/`。待機畫面也支援灰階 logo。
- **裝置端診斷紀錄**:SD 卡根目錄放一個空的 `diag.on` 檔即開啟，記錄翻頁耗時、
  逐記憶體池統計與配置失敗;不放就完全不執行。這台機器沒有序列埠，這是唯一的遙測管道。
- 品牌與網路識別改為 CrossMosa:`http://crossmosa.local`、熱點 `CrossMosa-Reader`、
  資料目錄 `/.crossmosa/`（從舊目錄自動搬遷，進度、書籤、Wi-Fi 憑證與快取全數保留）。

### 閱讀與效能 (Reading & Performance)

- **字型預取**:下一頁需要的字趁面板刷新的空檔先從 SD 卡讀好。
  面板推電泳粒子的那 441 ms 裡 CPU 本來是完全閒置的，而下一頁最貴的成本正是 250–300 ms 的
  SD 讀取。實測 320 次翻頁:命中的頁 render 中位 952 ms、未命中 1,278 ms，**省約 326 ms**。
- 抗鋸齒的兩個灰階平面**合併成單趟走訪**（14 趟 → 7 趟）。
- **有圖的書頁**改用更高的灰階分帶，SD 讀取量從 1,345 KB 降到 384 KB:整頁 5,849 ms → 2,422 ms。
- 從待機喚醒少一次強制全螢幕同步，約省 2.3 秒。
- 回主畫面時多餘的第二次全螢幕重繪移除，每次進主畫面省約 0.5 秒與一次閃爍。
- 週期性清殘影改用不觸發重新同步的波形:3,192 ms → 約 720 ms。
- 字型預載改成**按實際字面拆分**，SD I/O 與單次最大連續配置各減半。
- 字寬表改成「一次配滿上限、之後就地合併」，不再用成長搬遷——那會在記憶體池中間留下
  永久空洞，把可用的最大連續區塊從 115 KB 砍到 42 KB。
- 字型快取失手時的補畫路徑改用常駐檔柄，每個字 12–18 ms 降到約 3 ms
  （原本每個字都重開一次檔案，而開檔會把路徑從根目錄掃兩遍）;
  快取配不下時改成「丟掉最占空間的幾個字」而不是整組放棄。
- 背景章節重排提早起跑、視窗加大，長中文書後段翻頁不再卡住。
- 中文行距 WIDE 由 1.1 調整為 1.2。
- 新增每頁停留時間量測，作為判斷預取類優化值不值得做的依據。

### 穩定性 (Stability)

- **連 Wi-Fi 或開熱點前自動卸載 SD 內文字型**，騰出 30–90 KB。
  常駐的中文字型加上無線網路堆疊是這台機器最常見的記憶體不足來源。
- **CSS 規則依可用記憶體自動降級**。部分 EPUB 內嵌超過 100 KB 的 CSS，
  規則表膨脹會撐爆記憶體讓整本書開不了;現在會少收或不收規則，書照開。
- 章節建置的**全路徑記憶體護欄** + 配置失敗時的降級:記憶體不足時顯示「索引失敗」，
  不再直接重開機。
- 斷字分詞的六個平行陣列、斷行 DP 表與斷行索引表全部補上配置前的原子檢查。
  韌體用 `-fno-exceptions`，配置失敗時無法丟例外，會直接 `abort()`。
- **圖片尺寸改成直接讀檔頭**(PNG IHDR / JPEG SOF)，不再為了量長寬去配置 44 KB 的解碼器。
  先前在排版階段配不到就把圖丟掉，而且會固化進快取變成永久掉圖。
- 圖片解碼前記憶體不足時，可暫時卸載內文字型再重載。
- 抖動器的配置全面改為 nothrow，**且三個呼叫端都補上檢查**(只改配置端會從 abort 變成
  解參考空指標，一樣當機但更難查)。
- 啟用 wolfSSL 的 SP ECC 實作，TLS 握手的記憶體需求下降約 80%;
  先前對使用 ECDSA 憑證的伺服器，OPDS 連線只是剛好撐得住。
- 修正 OPDS 連線時的偶發 panic:框架在主任務裸呼叫 `dns_clear_cache()`，
  與在途的 NTP 網域查詢競態。修法是把該呼叫包進 TCP/IP 核心鎖的建置期修補，
  框架升級時會硬擋建置而不是無聲失效。
- 網頁設定頁改為批次送出（87 個極小 TCP 分塊 → 5 個），先前該頁面會卡住。
- 所有 JSON 設定落地統一走「寫暫存檔再改名」，並偵測寫入長度短少。
- 序列化字串長度守衛:超長時跳過內容維持串流同步，而不是截斷或配置失敗。
- 修正網頁檔案管理的路徑檢查:先前只比對最後一段檔名就開啟完整路徑，
  Wi-Fi 憑證檔可經由未驗證的 HTTP 下載。檢查改為比對經過檔案系統正規化後的完整路徑。

### 在地化 (Localization)

- **完整的繁體中文介面**:446 個 UI 字串全部翻譯，台灣用語。
- 逐字串對照實際畫面的情境複查，修正 31 條共用字串的誤譯
  （例如同一個字串同時用在「書名」與「進度列範圍」兩處而語意不合）。
- **內建 UI 字型改為含漢字的字型堆疊**:拉丁走 Ubuntu，漢字走 Noto Sans TC 子集，
  另含希伯來文、阿拉伯文與 U+FFFD 備援。
- UI 漢字涵蓋 **7,413 字**:BIG5 一級全部，加上 BIG5 二級中最常用的 2,000 字。
  選字規則可重現且完整記錄在字集檔的檔頭(候選池取自 1982 年教育部標準字體表，
  字頻只在池內排序;純頻率排序的尾端是死字，已否決)。
- 補齊全書庫目錄實際用到的字:注音符號與聲調、日文假名、圈圈數字、直排標點、
  ℃ 與 ★ 等符號，以及若干日文變體漢字。
- **修掉多處把中文畫進 8px 拉丁字型造成的方塊**:按鈕提示列、開機與休眠提示、
  副標題、值欄、鍵盤提示、Wi-Fi 與 Calibre 教學文字。
- 清單主文字由 10px 放大到 14px（移除 12px 字級換來的空間），列高相應加大。
- **CJK 逐字斷行**。
- 重組頁面文字時，CJK 字元之間不插空格（先前 QR 書摘與書籤摘要會變成「偉 大 力 量」）。
- **SD 卡內文字型三套**:Noto Sans TC（黑體，含粗體）、Noto Serif TC（明體，22,219 字全涵蓋）、
  芫荽 Iansui（硬筆楷書），各四個字級 16/18/20/22;另保留 IBM Plex Sans TC。
- 記錄並強制執行 SD 字型的 4,096 個 interval 硬上限——超過會整份拒載變全方塊，
  芫荽因此裁到 5,366 個常用字。
- 介面主題重整為自訂的 **Formosa**:統一的選取語言（圓角外框 + 左側豎條）、
  真正的頁籤形分頁列、e-ink 上不塗滿背景、彈窗置中、長標題折行。
- **全機圖示換裝 [Tabler Icons](https://github.com/tabler/tabler-icons) 3.31.0**(MIT):
  15 顆圖示統一線條品質，選圖刻意沿用換裝前的既有隱喻，不要求使用者重新學符號;
  32px 的線寬換算成 1.5、24px 維持 2（同一個 2px 螢幕線寬在兩種縮放比下的解）。
  來源 SVG 與換算規則收在 `icons/`。

### 與上游的差異 (Removed / disabled vs upstream)

移除的部分都是拔掉入口點讓連結器回收整條相依鏈，檔案多半還在樹上，主要目的是**騰 flash 給中文字型**。

- 移除 English 與繁體中文以外的 **29 種 UI 語言**（約 258 KB）。
- 移除 **XTC 格式**(`.xtc` / `.xtch`)支援——那是 X4 的原生預渲染格式。
- 移除 **KOReader 進度同步**與**字典查詢**(StarDict)——兩者本分支都用不到。
- 移除 **OTA 線上更新**:它指向上游的 release，誤按會把本分支蓋掉。
  **SD 卡韌體更新保留**，並可用「按住側邊上鍵開機」進入救援畫面。
- 移除 **Classic 與 RoundedRaff 主題**（保留 Formosa 與 Formosa Extended）。
- 移除**非英文的斷字表**（9 種語言，約 323 KB）——中文不斷字。
- **內建閱讀字型大幅精簡**（合計約 1.3 MB）:斜體字面移除並自動退回正體
  （SD 中文字型本來就只有正體與粗體）、內建 notoserif 字面移除
  （設定裡的「Serif」會顯示為黑體）、其餘縮到**單一 14px 備援**。
  這整組只在沒有 SD 字型時才用得到。
- **SMB2 伺服器**（iOS「檔案」App 直接管理 SD 卡）**預設不編入**。功能可用，
  實機驗證過完整的建立 / 寫入 / 改名 / 刪除，但整體體驗不佳而下架;原始碼與
  vendored 的 libsmb2 都保留，取消 `platformio.ini` 中一行註解即可編回來。
- **BLE 翻頁遙控器預設不編入**。連結 NimBLE 就是約 27 KB 的固定成本，
  加上執行期需求後，長章節每頁會掉數十個字。閱讀優先於配件，要用請編第二份韌體。
- **保留**:Calibre 無線推書（相容上游外掛生態）、網頁設定與檔案傳輸、WebDAV、
  OPDS、傾斜翻頁、螢幕截圖、按鍵重配、待機畫面模式。

### 資源用量 (Resource footprint)

| | 數值 |
|---|---|
| Flash（app 分割區 6.5 MB） | 5,955,789 bytes，90.9%，剩約 584 KB |
| 靜態 RAM | 53,484 bytes |
| UI 字型漢字數 | 7,413 |

### 可重現建置 (Reproducible builds)

設定 `SOURCE_DATE_EPOCH` 之後，`firmware.bin` 是**逐位元組可重現**的;不設的話
`__DATE__` / `__TIME__` 會讓兩次建置差幾十個位元組。每次發佈都會公佈當次使用的
epoch 與 sha256。做法見 `docs/reproducible-builds.md` 與 `scripts/mk-release.sh`。

---

### Internal build mapping

`1.0.0` = internal build **113**。裝置顯示的版本字串為 `1.0.0 (cp1.4.1 build 113)`，
其中 `cp1.4.1` 是這個分支所基於的上游 CrossPoint 版本。
